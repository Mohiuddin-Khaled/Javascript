// array method is flat()

// convert nested array into simple array

// nested array and multilevel parameter
// flattened array 

const nested = [1, 2, [3, 4], [5, 6]];
const flattened = nested.flat(2);
console.log(flattened);