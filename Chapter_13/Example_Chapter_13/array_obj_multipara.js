// map(), filter(), find(), forEach()

// Each array or object include three parameters
// const students = [
//     { name: "John", age: 20 },
//     { name: "Adam", age: 22 },
//     { name: "Tom", age: 19 },
//     { name: "Lucky", age: 21 }
// ];

// 3 parameters are element, index, length
// const studentInfo = students.map((student, index, arr) => {
//     return `${student.name}, ${index + 1} of ${arr.length}`;
// });

// console.log(studentInfo);

