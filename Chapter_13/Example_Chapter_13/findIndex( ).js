// using findIndex() method to get index(position) based on condition

// if required the condition then return element index otherwise return -1 (invalid index)
const numbers = [10, 25, 30, 50];
// const index = numbers.findIndex(num => num > 27);
// console.log(index);

// const veryBig = numbers.findIndex(num => num > 100);
// console.log(veryBig);

