// practice 1 page 236

// slice() elements of an array
// selected range

// slice 'Banana' and 'Cherry' from the array and store them in another array
const fruits = ['Apple', 'Banana', 'Cherry', 'Dates'];

const fruit = fruits.slice(1, 3);

console.log(fruit);
console.log(fruits);