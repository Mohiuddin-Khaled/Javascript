// practice 1 page 224

// create an array that return value is grater than 100
const numbers = [34, 67, 100, 393, 290, 150];

const isExists = numbers.some(num => num > 100);

console.log(isExists);