// practice 1 page 221

// Increase each array element by 13 using map and return the updated array
const arr = [30, 45, 20, 60, 10];

const updatedArr = arr.map(num => num + 13);

console.log(updatedArr);
