// log each element to the console of an array using forEach()

const numbers = [1, 2, 3, 4, 5];

const numList = numbers.forEach(num => console.log(num));

// console.log(numList);    // undefine