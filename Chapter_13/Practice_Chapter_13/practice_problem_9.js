// log each element to the console of an array using forEach()

const str = ['cow', 'goat', 'sheep', 'horse'];

const strList = str.forEach(st => console.log(st));

// console.log(strList);    // undefine


