// get string value from the array where each element is longer than 5 characters

const str = ['Messi', 'Maradona', 'Pele', 'Zidane', 'Ronaldo'];

const updatedSt = str.find(st => st.length > 5);

console.log(updatedSt);