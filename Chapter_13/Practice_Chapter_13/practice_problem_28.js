// slice() elements of an array
// selected range

// slice 'Tesla' and 'BMW' from the array and store them in another array
const cars = ['Tesla', 'BMW', 'Toyota', 'Ford'];
const car = cars.slice(0, 2);

console.log(car);