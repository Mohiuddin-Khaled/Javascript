// create a function that return nearest larger, smaller, round value 

function convert(num) {
    console.log(Math.ceil(num));

    console.log(Math.floor(num));

    console.log(Math.round(num));
}

// convert(4.645);

// convert(43.342);

convert(3.321);