// return destructured first and second value of an array

function multiply(a, b) {
    return [a * 3, b * 3];
};

const [first, second] = multiply(4, 23);

console.log(first);
console.log(second);

